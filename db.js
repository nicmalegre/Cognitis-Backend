/* const { Sequelize } = require('sequelize');

const sequelize = new Sequelize('dblll', 'root', 'root', {
    host: 'localhost',
    dialect: 'mysql'
});

try {
    await sequelize.authenticate();
    console.log('database connected');
} catch (error) {
    console.log('Error to try connect db', error);
} */